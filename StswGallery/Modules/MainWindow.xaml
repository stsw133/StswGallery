<se:StswWindow x:Class="StswGallery.MainWindow"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:drawing="clr-namespace:System.Drawing;assembly=System.Drawing"
               xmlns:se="clr-namespace:StswExpress.Wpf;assembly=StswExpress.Wpf"
               xmlns:local="clr-namespace:StswGallery"
               mc:Ignorable="d" d:DataContext="{d:DesignInstance local:MainContext}"
               Height="450" Width="750" WindowStartupLocation="CenterScreen" WindowState="Maximized"
               Title=""
               Loaded="{se:StswEventToCommand InitCommand}"
               PreviewKeyDown="{se:StswEventToCommand KeyPressCommand, PassEventArgs=True}">
    <se:StswWindow.Resources>
        <se:StswBindingProxy x:Key="proxy" Proxy="{Binding}"/>
    </se:StswWindow.Resources>

    <se:StswContentDialog CloseOnBackdropClick="True" Identifier="ConfigContentDialog" IsOpen="{Binding IsConfigOpen}" IsTabStop="False">
        <Grid>
            <!-- image -->
            <se:StswZoomControl x:Name="ZoomControl">
                <se:StswZoomControl.Style>
                    <Style TargetType="{x:Type se:StswZoomControl}">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding UseMediaPlayer}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </se:StswZoomControl.Style>
                <Image Source="{Binding ImageSource}" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant">
                    <Image.Style>
                        <Style TargetType="{x:Type Image}">
                            <Setter Property="StretchDirection" Value="DownOnly"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConfigContext.Settings.UseDefaultStretchDirection}" Value="True">
                                    <Setter Property="StretchDirection" Value="Both"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </se:StswZoomControl>
            <se:StswMediaPlayer Source="{Binding MediaSource}">
                <se:StswMediaPlayer.Style>
                    <Style TargetType="{x:Type se:StswMediaPlayer}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding UseMediaPlayer}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </se:StswMediaPlayer.Style>
            </se:StswMediaPlayer>
            <!-- default button -->
            <se:StswSubButton d:Foreground="White"
                              Command="{Binding SelectDirectoryCommand}"
                              Content="Click to select a directory..."
                              Focusable="False"
                              IsTabStop="False"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              Visibility="{Binding DirectoryPath, Converter={x:Static se:StswExistenceConverter.Instance}, ConverterParameter=null}"/>
            <!-- option panel -->
            <se:StswSidePanel Focusable="False"
                              IsAlwaysVisible="{Binding IsSidePanelLocked}"
                              MinHeight="50"
                              MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type Grid}}, Converter={x:Static se:StswCalculateConverter.Instance}, ConverterParameter='*0.99'}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Bottom"
                              HorizontalContentAlignment="Center">
                <se:StswBorder BorderBrush="{DynamicResource StswBorder.Static.Border}" BorderThickness="2 2 2 0">
                    <DockPanel Background="{DynamicResource StswBox.Static.Background}">
                        <!-- top side -->
                        <DockPanel DockPanel.Dock="Top">
                            <!-- left side -->
                            <se:StswButton se:StswControl.IsBorderless="True" Command="{Binding SelectDirectoryCommand}" Focusable="False" ToolTip="Select directory">
								<se:StswIcon Data="{x:Static se:StswIcons.FolderOpen}" Foreground="{DynamicResource StswColor.Structure}"/>
                            </se:StswButton>
                            <se:StswButton se:StswControl.IsBorderless="True" Command="{Binding RefreshCommand}" Focusable="False" ToolTip="Refresh file list">
								<se:StswIcon Data="{x:Static se:StswIcons.Refresh}" Foreground="{DynamicResource StswColor.Neutral}"/>
                            </se:StswButton>
							<se:StswSeparator Orientation="Vertical"/>
							<!-- right side -->
							<se:StswLabel DockPanel.Dock="Right"
                                          Content="{Binding CurrentFile.SizeText}"
                                          HorizontalContentAlignment="Center"
                                          IconData="{x:Static se:StswIcons.Weight}" IconScale="1.1"
                                          Width="80"/>
							<se:StswSeparator DockPanel.Dock="Right" Orientation="Vertical"/>
							<se:StswLabel DockPanel.Dock="Right"
                                          Content="{Binding CurrentFile.DimensionText}"
                                          HorizontalContentAlignment="Center"
                                          IconData="{x:Static se:StswIcons.ArrowTopRightBottomLeft}" IconScale="1.1"
                                          Width="120"/>
							<se:StswSeparator DockPanel.Dock="Right" Orientation="Vertical"/>
							<!-- center -->
							<se:StswTextBox se:StswControl.IsBorderless="True" FontSize="10" IsReadOnly="True" Text="{Binding DirectoryPath}" VerticalAlignment="Stretch"/>
                        </DockPanel>
                        <se:StswSeparator DockPanel.Dock="Top"/>
                        <!-- bottom side -->
                        <DockPanel DockPanel.Dock="Top">
                            <!-- left size -->
							<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding LockSidePanelCommand}" Focusable="False" ToolTip="Lock side panel" Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}">
								<se:StswIcon Scale="1.1">
									<se:StswIcon.Style>
										<Style TargetType="{x:Type se:StswIcon}">
											<Style.Triggers>
												<DataTrigger Binding="{Binding IsSidePanelLocked}" Value="False">
													<Setter Property="Data" Value="{x:Static se:StswIcons.LockOpen}"/>
												</DataTrigger>
												<DataTrigger Binding="{Binding IsSidePanelLocked}" Value="True">
													<Setter Property="Data" Value="{x:Static se:StswIcons.Lock}"/>
													<Setter Property="Foreground" Value="{DynamicResource StswColor.Structure}"/>
												</DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </se:StswIcon.Style>
                                </se:StswIcon>
							</se:StswButton>
							<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding ConfigCommand}" Focusable="False" ToolTip="Open settings">
								<se:StswIcon Data="{x:Static se:StswIcons.Cog}" Foreground="{DynamicResource StswColor.Neutral}"/>
							</se:StswButton>
							<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding ToggleDisplayModeCommand}" Focusable="False" ToolTip="{Binding DisplayModeText}">
								<se:StswIcon Scale="1.1">
									<se:StswIcon.Style>
										<Style TargetType="{x:Type se:StswIcon}">
											<Setter Property="Data" Value="{x:Static se:StswIcons.ArrowTopRightBottomLeft}"/>
											<Setter Property="Foreground" Value="{DynamicResource StswColor.Neutral}"/>
											<Style.Triggers>
												<DataTrigger Binding="{Binding IsDynamicDisplayMode}" Value="True">
													<Setter Property="Data" Value="{x:Static se:StswIcons.Refresh}"/>
													<Setter Property="Foreground" Value="{DynamicResource StswColor.Structure}"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</se:StswIcon.Style>
								</se:StswIcon>
							</se:StswButton>
							<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding RemoveFileCommand}" Focusable="False" ToolTip="Move current file to Recycle Bin">
								<se:StswIcon Data="{x:Static se:StswIcons.TrashCan}" Foreground="{DynamicResource StswColor.Negative}"/>
							</se:StswButton>
							<se:StswDropButton Focusable="False"
                                               ToolTip="Edit image"
                                               se:StswControl.IsBorderless="True"
                                               se:StswDropArrow.Visibility="Collapsed"
                                               se:StswPopup.Background="Transparent">
								<se:StswDropButton.Header>
									<se:StswIcon Data="{x:Static se:StswIcons.Pencil}" Foreground="{DynamicResource StswColor.Neutral}"/>
								</se:StswDropButton.Header>
								<StackPanel Orientation="Horizontal">
									<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding RotateFlipImageCommand}" CommandParameter="{x:Static drawing:RotateFlipType.Rotate90FlipNone}" Focusable="False" ToolTip="Rotate left">
										<se:StswIcon Data="{x:Static se:StswIcons.RotateLeft}"/>
									</se:StswButton>
									<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding RotateFlipImageCommand}" CommandParameter="{x:Static drawing:RotateFlipType.Rotate270FlipNone}" Focusable="False" ToolTip="Rotate right">
										<se:StswIcon Data="{x:Static se:StswIcons.RotateRight}"/>
									</se:StswButton>
									<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding RotateFlipImageCommand}" CommandParameter="{x:Static drawing:RotateFlipType.RotateNoneFlipX}" Focusable="False" ToolTip="Flip horizontally">
										<se:StswIcon Data="{x:Static se:StswIcons.FlipHorizontal}"/>
									</se:StswButton>
									<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding RotateFlipImageCommand}" CommandParameter="{x:Static drawing:RotateFlipType.RotateNoneFlipY}" Focusable="False" ToolTip="Flip vertically">
										<se:StswIcon Data="{x:Static se:StswIcons.FlipVertical}"/>
									</se:StswButton>
								</StackPanel>
							</se:StswDropButton>
							<!-- right side -->
							<StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right" Orientation="Horizontal">
								<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding PreviousFileCommand}" Focusable="False" ToolTip="Previous file">
									<se:StswIcon Data="{x:Static se:StswIcons.ArrowLeft}"/>
								</se:StswButton>
								<se:StswButton se:StswControl.IsBorderless="True" Command="{Binding NextFileCommand}" Focusable="False" ToolTip="Next file">
									<se:StswIcon Data="{x:Static se:StswIcons.ArrowRight}"/>
								</se:StswButton>
							</StackPanel>
							<se:StswSeparator DockPanel.Dock="Right" Orientation="Vertical"/>
							<se:StswLabel DockPanel.Dock="Right"
                                          Content="{Binding ZoomPercentage, ElementName=ZoomControl}" ContentStringFormat="0\%"
                                          HorizontalContentAlignment="Center"
                                          IconData="{x:Static se:StswIcons.MagnifyPlus}" IconScale="1.1"
                                          Width="100"/>
							<se:StswSeparator DockPanel.Dock="Right" Orientation="Vertical"/>
                                <se:StswLabel DockPanel.Dock="Right"
                                              Content="{Binding FileCounterText}"
                                              HorizontalContentAlignment="Center"
                                              Width="100"/>
                                <se:StswSeparator DockPanel.Dock="Right" Orientation="Vertical"/>
                            <!-- center -->
                            <ItemsControl HorizontalAlignment="Center" ItemsSource="{Binding ConfigContext.Settings.Shortcuts}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <se:StswButton se:StswControl.IsBorderless="True"
                                                       Command="{Binding Proxy.KeyNumberCommand, Source={StaticResource proxy}}"
                                                       CommandParameter="{Binding Number}"
                                                       Focusable="False"
                                                       ToolTip="Run number shortcut"
                                                       Margin="0">
                                            <se:StswLabel Content="{Binding Number}" HorizontalContentAlignment="Center" Width="20"/>
                                        </se:StswButton>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DockPanel>
                    </DockPanel>
                </se:StswBorder>
            </se:StswSidePanel>
        </Grid>
    </se:StswContentDialog>
</se:StswWindow>
